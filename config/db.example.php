<?php
/**
 * FEDI (Architect): Database config template.
 *
 * TEAM: we had config/db.php in .gitignore (to avoid pushing
 * credentials) but never created the file or this example. Fair point.
 *
 * Either way, to be fully committed to the deal,
 * we're adding the /config structure. Teammates: copy this to db.php and set
 * your WAMP credentials. Database.php loads from there.
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'ad_attack_db');
define('DB_USER', 'root');
define('DB_PASS', '');
